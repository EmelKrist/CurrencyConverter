# Data source configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/service_client_db
spring.datasource.username=postgres
spring.datasource.password=895271

# Hibernate configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Port for deploy
server.port=8080

# Currency conversion service configuration
microservice-currency-conversion.url=http://localhost:8081/conversions