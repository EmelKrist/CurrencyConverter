# Data source configuration
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.url=jdbc:postgresql://localhost:5432/service_currency_conversion_db
spring.datasource.username=postgres
spring.datasource.password=895271

# Hibernate configuration
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# REST API for exchange rates
exchange.rate.api.url=https://api.coingate.com/api/v2/rates/merchant

# server configuration
server.port=8081